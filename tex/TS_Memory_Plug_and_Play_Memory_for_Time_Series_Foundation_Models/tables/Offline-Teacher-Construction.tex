% \begin{algorithm}[t]
% \caption{Offline Teacher Construction}
% \label{alg:teacher}
% \begin{algorithmic}[1]
% \Require training split $\mathcal{D}_{\text{train}}$, window length $L$, horizon $H$, neighbors $K$,
%         frozen encoder $f_{\text{enc}}$, temperature $\tau_{\text{ret}}$
% \Ensure teacher dataset $\mathcal{D}_{\text{teach}}$

% \State Fit scaler on $\mathcal{D}_{\text{train}}$ and build leakage-safe knowledge base $\mathcal{K}$
% \State Pre-compute embeddings $\mathbf{e}_i \gets f_{\text{enc}}(\mathbf{X}^{(i)})$ for all entries in $\mathcal{K}$
% \State $\mathcal{D}_{\text{teach}} \gets \emptyset$

% \For{each supervised window $(\mathbf{X}_t,\mathbf{Y}_t)$}
%     \State $\mathbf{e}_t \gets f_{\text{enc}}(\mathbf{X}_t)$ and retrieve candidates by nearest neighbors
%     \State Remove index-matched neighbors; align candidates using trailing-mean shift; rerank by shifted-context $\ell_1$
%     \State Keep top-$K$ neighbors and compute weights $w_k \propto \exp\!\big(-\psi(d_k)/\tau_{\text{ret}}\big)$
%     \State Compute weighted-quantile teacher $\widetilde{\mathbf{Q}}_t$ and confidence $\mathrm{Conf}_t \gets \max_k w_k$
%     \State Add $(\mathbf{X}_t,\mathbf{Y}_t,\widetilde{\mathbf{Q}}_t,\mathrm{Conf}_t)$ to $\mathcal{D}_{\text{teach}}$
% \EndFor
% \end{algorithmic}
% \end{algorithm}


% \begin{algorithm}[t]
% \caption{Offline Teacher Construction}
% \label{alg:teacher}
% \begin{algorithmic}[1]
% \Require training split $\mathcal{D}_{\text{train}}$, context length $L$, horizon $H$, neighbors $K$,
%         trailing length $m$, frozen encoder $f_{\text{enc}}$, temperature $\tau_{\text{ret}}$, distance transform $\psi(\cdot)$
% \Ensure teacher dataset $\mathcal{D}_{\text{teach}}$

% \State Fit scaler on $\mathcal{D}_{\text{train}}$ and standardize all extracted windows
% \State Build leakage-safe knowledge base $\mathcal{K}=\{(\mathbf{X}^{(i)},\mathbf{Y}^{(i)},i)\}_{i=1}^{N_{\text{train}}}$ from $\mathcal{D}_{\text{train}}$
% \State Pre-compute embeddings $\mathbf{e}_i \gets f_{\text{enc}}(\mathbf{X}^{(i)})$ for all entries in $\mathcal{K}$
% \State Build a nearest-neighbor index over $\{\mathbf{e}_i\}$ in embedding space
% \State $\mathcal{D}_{\text{teach}} \gets \emptyset$

% \For{each supervised window $(\mathbf{X}_t,\mathbf{Y}_t,t)$}
%     \State $\mathbf{e}_t \gets f_{\text{enc}}(\mathbf{X}_t)$
%     \State Retrieve a candidate pool $\mathcal{N}$ by nearest-neighbor search using $d_i=\|\mathbf{e}_t-\mathbf{e}_i\|_2$
%     \State Remove index-matched candidates: $\mathcal{N} \gets \mathcal{N}\setminus\{t\}$

%     \For{each candidate index $i\in\mathcal{N}$}
%         \State $\delta_i \gets \mathrm{mean}(\mathbf{X}_{t,L-m+1:L})-\mathrm{mean}(\mathbf{X}^{(i)}_{L-m+1:L})$
%         \State $\mathbf{Y}^{(i)}_{\text{align}} \gets \mathbf{Y}^{(i)}+\delta_i$
%         \State $r_i \gets \|\mathbf{X}_t-(\mathbf{X}^{(i)}+\delta_i)\|_1$
%     \EndFor

%     \State Select top-$K$ indices $\{i_k\}_{k=1}^{K}$ with smallest $r_i$
%     \State Compute weights
%     \[
%         w_k \gets \frac{\exp\!\left(-\psi(d_{i_k})/\tau_{\text{ret}}\right)}
%         {\sum_{j=1}^{K}\exp\!\left(-\psi(d_{i_j})/\tau_{\text{ret}}\right)}
%     \]
%     \State Compute weighted-quantile teacher $\widetilde{\mathbf{Q}}_t$ from $\{\mathbf{Y}^{(i_k)}_{\text{align}}\}_{k=1}^{K}$ under weights $\{w_k\}_{k=1}^{K}$ at levels $\mathcal{Q}$
%     \State $\mathrm{Conf}_t \gets \max_{1\le k\le K} w_k$
%     \State Add $(\mathbf{X}_t,\mathbf{Y}_t,\widetilde{\mathbf{Q}}_t,\mathrm{Conf}_t)$ to $\mathcal{D}_{\text{teach}}$
% \EndFor

% \State \Return $\mathcal{D}_{\text{teach}}$
% \end{algorithmic}
% \end{algorithm}

% \begin{algorithm}[htbp]
% \caption{Offline Teacher Construction}
% \label{alg:teacher}
% \begin{algorithmic}[1]
% \Require training split $\mathcal{D}_{\text{train}}$, context length $L$, horizon $H$, neighbors $K$,
%         frozen encoder $f_{\text{enc}}$, temperature $\tau_{\text{ret}}$
% \Ensure teacher dataset $\mathcal{D}_{\text{teach}}$

% \State Fit scaler on $\mathcal{D}_{\text{train}}$; build leakage-safe knowledge base $\mathcal{K}$ from training windows
% \State Pre-compute embeddings $\mathbf{e}_i \gets f_{\text{enc}}(\mathbf{X}^{(i)})$ for all entries in $\mathcal{K}$ and build an NN index
% \State $\mathcal{D}_{\text{teach}} \gets \emptyset$

% \For{each supervised window $(\mathbf{X}_t,\mathbf{Y}_t)$}
%     \State $\mathbf{e}_t \gets f_{\text{enc}}(\mathbf{X}_t)$; retrieve candidates $\mathcal{N}$ by nearest neighbors in embedding space
%     \State Remove index match; align candidates by trailing-mean shift and rerank by shifted-context $\ell_1$; keep top-$K$
%     \State $\mathbf{w} \gets \mathrm{Softmax}\!\big(-\psi(\mathbf{d})/\tau_{\text{ret}}\big)$; $\mathrm{Conf}_t \gets \max_k w_k$
%     \State $\widetilde{\mathbf{Q}}_t \gets$ weighted empirical quantiles of aligned futures under $\mathbf{w}$ at levels $\mathcal{Q}$
%     \State Add $(\mathbf{X}_t,\mathbf{Y}_t,\widetilde{\mathbf{Q}}_t,\mathrm{Conf}_t)$ to $\mathcal{D}_{\text{teach}}$
% \EndFor

% \State \Return $\mathcal{D}_{\text{teach}}$
% \end{algorithmic}
% \end{algorithm}


% \begin{algorithm}[!htbp]
% \caption{Offline Teacher Construction}
% \label{alg:teacher}
% \begin{algorithmic}[1]
% \Require training split $\mathcal{D}_{\text{train}}$, context length $L$, horizon $H$, quantile levels $\mathcal{Q}$,
%         neighbors $K$, trailing length $m$, frozen encoder $f_{\text{enc}}$,
%         distance transform $\psi(\cdot)$, temperature $\tau_{\text{ret}}$
% \Ensure teacher dataset $\mathcal{D}_{\text{teach}}$

% \State Build leakage-safe knowledge base $\mathcal{K}=\{(\mathbf{X}^{(i)},\mathbf{Y}^{(i)})\}_{i=1}^{N_{\text{train}}}$
% \State Pre-compute $\mathbf{e}_i \gets f_{\text{enc}}(\mathbf{X}^{(i)})$ for all entries in $\mathcal{K}$ and build an NN index
% \State $\mathcal{D}_{\text{teach}} \gets \emptyset$

% \For{each supervised window $(\mathbf{X}_t,\mathbf{Y}_t)$}
%     \State $\mathbf{e}_t \gets f_{\text{enc}}(\mathbf{X}_t)$; retrieve candidates $\mathcal{N}$ with distances $\{d_i\}$
%     \State Remove index-matched neighbors from $\mathcal{N}$
%     \State Align candidates by trailing-mean shift over last $m$ steps and rerank by shifted-context $\ell_1$; keep top-$K$
%     \State $w_k \gets \mathrm{Softmax}\!\big(-\psi(d_k)/\tau_{\text{ret}}\big)$; $\mathrm{Conf}_t \gets \max_k w_k$
%     \State $\widetilde{\mathbf{Q}}_t \gets$ weighted empirical quantiles of aligned futures under $\{w_k\}$ at levels $\mathcal{Q}$
%     \State Add $(\mathbf{X}_t,\mathbf{Y}_t,\widetilde{\mathbf{Q}}_t,\mathrm{Conf}_t)$ to $\mathcal{D}_{\text{teach}}$
% \EndFor

% \State \Return $\mathcal{D}_{\text{teach}}$
% \end{algorithmic}
% \end{algorithm}


% % \documentclass{article}
% % \usepackage{amsmath,amssymb}
% % \usepackage{algorithm}
% % \usepackage{algpseudocode}


\begin{algorithm}[!htbp]
\caption{Offline Teacher Construction}
\label{alg:teacher}
\begin{algorithmic}[1]
\Require training split $\mathcal{D}_{\text{train}}$, quantile levels $\mathcal{Q}$, neighbors $K$, trailing length $m$,
        frozen encoder $f_{\text{enc}}$, distance transform $\psi(\cdot)$, temperature $\tau_{\text{ret}}$
\Ensure teacher dataset $\mathcal{D}_{\text{teach}}$

\State $\mathcal{K} \gets \mathcal{D}_{\text{train}}$ \Comment{leakage-safe knowledge base}
\State Pre-compute $\mathbf{e}_i \gets f_{\text{enc}}(\mathbf{X}^{(i)})$ for all $(\mathbf{X}^{(i)},\mathbf{Y}^{(i)})\in\mathcal{K}$; build an NN index on $\{\mathbf{e}_i\}$
\State $\mathcal{D}_{\text{teach}} \gets \emptyset$

\For{each supervised window $(\mathbf{X}_t,\mathbf{Y}_t)$ in $\mathcal{K}$}
    \State Query NN index with $\mathbf{e}_t \gets f_{\text{enc}}(\mathbf{X}_t)$ to get candidates $\mathcal{N}_t$ and distances $\{d_k\}$; drop self-matches
    \State Align candidates by shifting the last-$m$ context mean; rerank by aligned-context $\ell_1$ and keep top-$K$
    \State $w_k \gets \mathrm{Softmax}\!\big(-\psi(d_k)/\tau_{\text{ret}}\big)$; $\mathrm{Conf}_t \gets \max_k w_k$
    \State $\widetilde{\mathbf{Q}}_t \gets$ weighted empirical quantiles of aligned futures under $\{w_k\}$ at levels $\mathcal{Q}$
    \State $\mathcal{D}_{\text{teach}} \gets \mathcal{D}_{\text{teach}} \cup \{(\mathbf{X}_t,\mathbf{Y}_t,\widetilde{\mathbf{Q}}_t,\mathrm{Conf}_t)\}$
\EndFor

\State \Return $\mathcal{D}_{\text{teach}}$
\end{algorithmic}
\end{algorithm}


